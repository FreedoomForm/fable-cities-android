# Native Android Port

## Current milestone

The repository ships a **dual-track APK**:

- **Track A — web-parity build (`ParityActivity`, secondary track).** The user's acceptance criterion is a 1:1 visual match with the browser game ("сохранив визуал один в один"). The browser game's look is produced by a three.js PBR + CSM + bloom + SMAA stack that a hand-written GLES renderer can not reproduce quickly, so the full production web build (`vite build` output, ~208 MB incl. CC0 texture sets and models) is bundled into the APK at CI build time (`android/app/src/main/assets/web/`, generated — never committed) and served offline through `WebViewAssetLoader` (`https://appassets.androidplatform.net/assets/web/`). ES modules, fetch and localStorage need a real http(s) origin, which `file://` cannot provide. Touch mapping: one finger = the page's native pointer behaviour (tools, UI); an injected shim maps pinch → wheel (zoom) and two-finger drag → middle-button drag (pan, the desktop camera binding); the Android Back button dispatches Escape (menus). This track is not a substitute for the native renderer — it is the user-facing visual-parity deliverable and is explicitly allowed by the user's clarified priority, superseding the original "no WebView at all" reading for this track only. ENTRY POINT UPDATE: the native GLES renderer (`MainActivity`) is now the LAUNCHER — the app icon opens the native build; the web-parity build opens from the native HUD "WEB-PARITY" chip, and Back twice (Esc dispatch + finish) returns to native.
- **Track B — native GLES renderer (`MainActivity`, adb-launchable, CI-gated).** A real Android `Activity`, an OpenGL ES 3.0 renderer, and a Canvas HUD overlay. `GlCityRenderer` renders through a GLES 3.0 pipeline on a `GLSurfaceView`: a deterministic seeded 96×96 terrain heightfield, a coastal water plane with animated Fresnel shading, a ray-marched sky gradient, distance fog, ~a hundred seeded buildings whose windows glow warm at night, and 26 deterministic vehicles. It computes a bounded 16:9 viewport at runtime and letterboxes rather than stretching. A Canvas-drawn HUD overlay provides large labelled touch controls above the GL surface. Porting the full three.js visual stack natively (PBR, cascaded shadow maps, GTAO, bloom, SMAA) remains the long-term goal of this track and is worked in parity slices.

## UI conversion decisions

The native HUD uses large labelled controls instead of the browser game's dense desktop toolbar. The essential tools are Select, Road, Zone, Service, and Bulldoze. Each tool is reachable from the bottom dock, reports its active state, provides a short action hint, and can be operated without a mouse, keyboard, hover state, or right-click. One-finger drag pans, pinch zooms, two-finger twist rotates, two-finger vertical drag pitches, and single taps apply the active tool through ray picking. Tool actions give immediate visible feedback through the message panel and city counters.

This is intentionally a redesign rather than a pixel-for-pixel port. A desktop layout can render on a phone while still requiring cursor precision, hover discovery, or keyboard shortcuts. Those interactions are not reliable on a touch screen, especially when the player is holding the device in landscape. The native layout therefore reserves a large bottom interaction band, keeps the city visible above it, uses explicit labels, and gives each action a pressed/selected visual state.

## Next port slices

1. Exercise both tracks on an emulator/device: CI runs an `emulator-smoke` job on every push (API 34 x86_64, KVM, swiftshader GPU): it installs the APK, boots the native launcher activity (60 s soak — the native build bakes control maps/splat arrays before the first frame; gates: process alive, zero fatal/native-signal lines, zero `GlCityRenderer` shader/link/GL-error lines, screenshot), then force-stops and boots the web-parity `ParityActivity` by component name (90 s soak — three.js first boot is slow on swiftshader; gates: process alive, zero page console errors, screenshot). An APK that fails either gate is never published. DONE: parity-track twist→rotate; rotation policy (landscape + configChanges); GATE 3 adb-driven rotation + HOME background/resume stress (process alive, zero fatals) on every push. Still open: real-device frame pacing.
2. **Native world parity — DONE (round 2).** `worldgen/` is a bit-exact Kotlin port of the browser game's world core: `shared/random.js` (mulberry32 + hash2), `shared/noise.js` (simplex 2D/3D, fbm, ridged), and `terrain/Heightmap.js` (the full analytic generator, grid generation, `getHeight`/`getSlope`, `flattenRect`/`conformPath`/`conformDisc`, `averageHeight`). Golden JVM tests (`WorldGenParityTest`) pin RNG/hash2/simplex/sampleGen/grid/conformPath to values produced by the actual web code in Node (seed 1337) and run on every CI push. The native scene renders the site's real world: coastal plain, meandering river, sea to the south, terraced mountains north; the prebuilt avenue/streets conform the terrain through the site's own `conformPath`; buildings, vehicles and the tool grid sit on real heights. Not yet ported from Heightmap.js: `raycast` (the renderer uses its own ray-march), `computeShoreDistance`, and the outer horizon-ring coarse grids.
3. **Native demo city + simulation — DONE.** `DemoCity.kt` ports the web `DemoCity.js` (u,v) road layout, trumpet interchange, block plan and routes bit-exactly (site picker pinned by `SitePickerParityTest` goldens). `Simulation.kt` ports the web simulation core: `services.js` (16 m coverage grids, capacity strain, upkeep), `economy.js` (household occupancy model, jobs, RCI demand, happiness, weekly budget) and `milestones.js` (population thresholds with rewards, alerts). `SimulationParityTest` runs the REAL web modules headless in Node (probe_sim.mjs) with the site's own sim RNG chain (`world.rng.fork(hashString('simulation'))`) and pins checkpoints at 24/72/168/336 game hours. In-game: 3 game minutes per real second (the site's secondsPerHour=20), demo blocks + painted zones feed the economy, the SERVICE tool places the site's 8 real service types at their real costs, road upkeep uses the web ROAD_COST table, the HUD shows live money/population/milestone from the model, and milestones pay real cash rewards.
4. **Native environment — DONE (clear weather).** `Environment.kt` ports `environment/atmosphere.js` bit-exactly (single-scattering sky, transmittances, sun+moon ephemeris, exposure curve, nightFactor) plus the clear-weather branch of `environment/index.js` computeFrame/sampleSkyAverages (sun/moon intensity + colour keys, hemisphere sky/ground, twilight floor, golden-hour tints, aerial-perspective fog). `EnvironmentParityTest` pins 9 probed hours + direct sky-radiance probes against Node goldens at the site's defaults (hour 14, doy 121, lat 47.3, clear preset). The renderer's invented sun state is gone; sky dome, fog, water and window lights are driven by the real model. Open: weather transitions (cloudy/rain/fog/snow), volumetric clouds, star field.
5. **Terrain ground — DONE (full 8-layer splat).** The vertex-colour approximation was retired. `TerrainShaders.FS_TERRAIN` is the FULL `TerrainMaterial.js` splat fragment shader (in-map branch) ported to GLSL ES 3.0: CPU control map (dry/dirt/sand/rockBoost | canopy/field/curvature), per-pixel world-normal slope, signed shore-distance band, two-scale anti-tiling + macro re-tint, triplanar dual-rock strata + lichen + cut-bank clay, wandering aspect-dependent snow line + patches, near-field 0.55 m + 2.2 m grass micro-layers, mid scree, curvature/drainage/terracette geology, rain wetness on albedo AND roughness (GGX sun lobe), wet shore band, night matte/cool grade + moon wrap, albedo ceiling, tNightFill + hemi floor + day sky-bounce fills. `TerrainGfx` loads the 8 CC0 PBR layers from the APK assets into two GL_TEXTURE_2D_ARRAYs (albedo|AO, normal|roughness; 512 px phone tier, mipmap + anisotropy) and bakes the terrain/index.js control map (512×512) + fillNormals world-normal map. `worldgen/GroundControl.kt` is a bit-exact `GroundControl.js` port pinned by `GroundControlParityTest` (FULL 512×512 map sums + probe rows + double rules vs `tools/probe_groundcontrol.mjs` on the real module). `Heightmap.kt` gained the CoarseGrid outer/far horizon rings + real `getHeightAny` (the control stencil crosses the map edge), public `relief()` and `riverCurvature()`; `worldgen/V8Math.kt` ports V8's Math.hypot 2/3-arg fast paths (fdlibm differs ~1 ulp on ~38 % of inputs) and the shore index uses jsRound (Math.round = floor(x+0.5), NOT rint).
6. **Parity APK size — DONE.** CI downscales every bundled texture to 1024 px after `vite build` (measured 187 → 42.7 MB on the real asset set; formats and filenames untouched so GLTF references keep resolving). The web-parity APK drops from ~200 MB to well under 100 MB.
7. **Native water — DONE (clear weather).** `worldgen/Water.kt` ports the web water stack bit-exactly (pinned by `WaterParityTest` against `tools/probe_water.mjs` goldens): `Heightmap.computeShoreDistance` (exact Felzenszwalb EDT shoreline SDF, Uint8 payload 128 + 4·sd), `textures.js makeNoiseTexture` (tileable RGBA fBm) and `makeWaterNormalTexture` (18 deterministic directional waves), the `Water.js _buildGeometry` quad scan (128 m chunks dipping below waterLevel + 0.8 plus the 4 horizon-ring quads) and `THREE.DataUtils.toHalfFloat`. `GlCityRenderer` renders the site's water through the same physical structure: per-pixel depth from an R16F height texture (dark teal → navy absorption ramp on 0x13282f, river bed in the first metre, 0.30 desaturation), five ripple octaves from the site's own 18-wave normal map (150 m swell → 2.4 m chop, the 23/61 m layers never fade), fresnel-weighted analytic sky probe desaturated to the site's slate chroma, a tight GGX glitter lobe for the sun AND the real moon, the noise-broken ≤ 1.6 m foam lace + rare whitecaps driven by the shore SDF, the absolute sky-bounce floor and luminance-normalised night sheen, and the dithered waterline dissolve. Planar reflection RT and the rain terms are DONE; waterline SDF recompute after terrain edits is DONE (the ROAD tool grades the corridor and the terrain:changed pipeline refreshes the R16F height + R8 shore textures with the cache invalidated).
8. **Native night sky — DONE.** `worldgen/Stars.kt` ports `environment/StarField.js` bit-exactly (pinned by `StarParityTest` against `tools/probe_star.mjs` goldens): the Milky Way cube map (galactic frame tilted 62.9°, seeded roll, dust lanes via the site's own simplex fBm, warm bulge, chroma+alpha encode at STAR_ALPHA_SCALE 0.7) and the 512×256 moon albedo map (highlands, maria, 180 craters with floors/rim/ejecta, pole longitude-stretch). `GlCityRenderer`'s sky now draws the site's full night: the procedural star field (pixel-exact cube-face grid with spectral colours, magnitude distribution and twinkle) plus the baked Milky Way rotating with the REAL sidereal angle (`Environment.EnvState.siderealAngle`), washed out by sky luminance exactly like the web; the phase-correct moon disc (limb darkening, earthshine, uMoonBright) with its triple-exponential halo; and the sun disc with limb darkening and the 2200 K golden-hour tint. Star wash under the cloud deck (uStarFade = 1 − 0.6 × cover) and per-pixel cloud occlusion of the sky (the deck composites over the stars) are DONE.
9. **Native traffic — DONE (full LaneNetwork + junction control).** `worldgen/Traffic.kt` ports the web traffic core bit-exactly: `LaneNetwork.js` makePoly/polyAt (Float32 polylines with the LAT_ACC curvature speed limit and backward apex-braking smoothing), `VehicleModels.js` VEHICLE_SPECS (6 types, weights, dims, vmax, wheel radius), the IDM car-following constants and accel, the weighted type picker, clear-check spawning, route extension with the `_astar` budget and its exact rng-stream semantics, spatial buckets, the leader search (same lane + up to 4 route elements ahead / 85 m) and the full per-vehicle integration (curve entry speed, brake lamps, wheel spin, steering, indicators, element transitions). The FULL LaneNetwork class is also ported bit-exactly: Bézier connectors through junctions, junction records (approaches, phase grouping, the FNV-1a desync timer for signal cycles, the connector conflict matrix, needsControl), the rank-weighted spawn table, the binary-heap A* router (Float32 g-costs + epoch stamps) and `updateSignals`. TrafficSim grew the web's junction control: red-light stop distances with amber run-through, per-node FCFS claims ordered by turn priority and road rank, blocked-exit refusal (never enter a junction whose exit is full), claim release on transitions, despawn-far and turn indicators. Pinned by `TrafficParityTest` (closed-loop IDM sim) AND `LaneNetworkParityTest` (a signalised 4-way + signalised T + priority crossing + dead-end U-turns world: element graph, junction records, conflict bytes, A* paths, 600 signal frames, 900 driven frames with claims/checkpoints) against `tools/probe_traffic.mjs` + `tools/probe_lanenetwork.mjs` running the REAL web modules. On device, `worldgen/RoadNetBuilder.kt` turns the 50 demo street polylines into a routable lane graph with the site's ROAD_TYPES cross-sections and classify() connection rules (182 nodes / 466 lanes / 1109 elements / 17 signalised + 43 priority junctions / 67.6 lane-km); the renderer runs the web's targetCounts spawn/despawn orchestration (native fleet cap 96 for frame time; despawn anchored at the look-at target because the native camera orbits 300+ m out). Pedestrian network + sim — DONE: `LaneNetwork._buildPedestrians` (1.45 m/s sidewalk lanes + Bézier crossing connectors with the gap > 34 m / crossing > 5.5 m rules) and the TrafficSim pedestrian agent (`spawnPeds`, `_stepPed` personal space + kerb waiting on `_crossingClear`, `_pickPedNext` back-avoidance) are ported bit-exactly and pinned by the same LaneNetworkParityTest goldens (18 ped lanes / 48 ped connections / 10 driven peds on the synthetic world; on the demo city: 360 sidewalk lanes, 93 road crossings, 50 lane-km). The renderer draws the walkers (two-box torso+head VBO) and runs the web's targetCounts() pedestrian fleet curve (native cap 64). Per-type vehicle meshes, brake + turn-indicator bulbs (the sim's blink/blinkSide flash phase driving amber front/rear glows) and planar reflection are DONE; vehicles ride the shared building shader's WetSurfaces grade.
10. **Native weather — DONE.** `worldgen/Weather.kt` ports `environment/Weather.js` bit-exactly (pinned by `WeatherParityTest` against `tools/probe_weather.mjs` running the REAL web class on seed 1337): all five presets, the damp() smoothing of 12 state keys + temp + precipitation, wetness (+dt/18, −dt/120) and snow-cover (+dt/45, −dt/240) accumulation, the deterministic wind wander (SimplexNoise seed^0x77ea) and the preset-pinned driftWind. `Environment.compute` grew the FULL weather branch of `computeFrame` (pinned by `EnvWeatherParityTest` + `tools/probe_env_weather.mjs`: 5 presets × 4 hours, 63 values per row): turbidity in every transmittance/sky sample, sunFactor on the sun & moon keys, overcast nightFactor (window lights come on in storms), cover/precipitation exposure, blocked-sunlight diffuse skylight, snow ground albedo, milk fog medium, fog density/height/floor/sun-glow/ceiling, directional aerial perspective and wetness. Clear-weather values are bit-identical to the previous port (the existing EnvironmentParityTest goldens still pass). `GlCityRenderer` runs the site's weather: `Weather(1337)` driven by the game clock (`(totalDays*24+hour)*3600`), fog density/height uniforms on every material, wet-surface darkening + snow whitening (WetSurfaces hooks), the SkyDome uSkyFog dissolve into the luminous fog colour with the forward-scatter sun glow (shaders.js block), the water uRain dimples + real wind, a Precipitation.js-style camera-following rain/snow particle volume (GPU-wrapped seeds, snow sway, distance fade) and a HUD weather chip cycling clear→cloudy→rain→fog→snow with the site's ~10 s smooth transitions. 11. **Native congestion stats + frame-time + planar reflection — DONE.** `TrafficSim.writeSegmentLoads` (the web's per-tick congestion pass: per-segment vehicle counts weighted by the slow-down beyond normal urban stop-and-go, capacity = length/22 × lanes, the length/lane-count metadata coming from `RoadNetBuilder` through `LaneGraphIn.segLength/segLaneCount`) fills `segLoad`/`congestion`/`avgSpeedRatio`; the HUD shows a live TRAFFIC % chip. Frame-time instrumentation: a rolling 240-frame window (avg ms + 99th-percentile 1%-low) logged every 10 s and exposed via `frameStats()`. Planar reflection RT: `renderReflection` mirrors the camera across the water plane (three Reflector math: mirrored eye/target, flipped up), builds the bias×proj×viewInv projective texture matrix, applies the Lengyel oblique near-plane clip, renders terrain/city ground/buildings/vehicles into a half-res RGBA8+depth FBO (alpha 0 = sky-probe fallback, mirrored-winding cull flip), and the water shader overrides its analytic sky reflection with the projective sample, normal-distorted and border-faded exactly like `Water.js`. Temporal cloud history (7f5b510) and the GroundFXPass screen-space scene reflections (6aaeb11, composited over the puddle pixels with the drainage-map pool sharpening) are DONE. The web-track STATUS.json quality snapshot ships from the CI browser loop as a release-adjacent artifact.

## Verification status

This environment has no Android SDK, Gradle executable, or ADB/emulator. The GLES 3.0 renderer, GLSurfaceView conversion, HUD overlay, gesture handling, and persistence were reviewed line-by-line for API and Kotlin consistency, and the browser modules continue to pass `npx vite build`. Android compilation is verified by the GitHub Actions workflow on every push, which also publishes the APK as a commit-linked prerelease. Installation, on-device screenshots, frame-count verification, and crash monitoring now run in CI on every push via the emulator gate; rotation/background-resume stress and real-device frame pacing remain explicit follow-up gates and must not be reported as passed until exercised.

13. **PuddleField — DONE.** `worldgen/PuddleField.kt` is a bit-exact port of `effects/PuddleField.js`: gutter pools in the camber + wheel-rut pools built from the REAL road network (FNV-1a segment id ^ seed ^ 0x9d2b → mulberry32), the world-space drainage map raster (R pool / G ploughed tyre band / B corridor), camber slope probes and the merged feathered-disc geometry (RING 8, CORE 0.84, LIFT 0.022, per-vertex alpha shore band) — pinned by `PuddleParityTest` against `tools/probe_puddles.mjs` running the REAL web module (pools, map sums/weighted checksum, geometry float sums + first-disc floats + centres). Renderer: `FS_PUDDLE` ports `PUDDLE_PARS`+`PUDDLE_INJECT` (surface wobble, expanding rain rings near the camera, dry/far alpha fade, `uPudWet` lifecycle `clamp((wet−0.12)/0.35)`) with a sky-Fresnel mirror + sun glint, drawn blended with depthWrite off and polygon offset (−4,−6) after the vehicles; `FS_LIT_WET` replaces the flat lit shader with the `WetSurfaces.js` wet branch (drainage-map puddle field + damp rim + Fresnel sky sheen) and snow branch (noise coverage + ploughed tyre bands from the drainage G channel) for the city ground/roads, and fixes `uWetness`/`uSnow`/cloud-shadow uniforms that were declared but never fed on that path. The GroundFX blit's SSR (scene colour + depth march, analytic lamp/head/tail emitter streaks) composites over the puddle pixels, so the standing water now carries the real scene image — closed.

14. **Native vegetation clearing + undergrowth — DONE.** `Vegetation.Forest` ports the clearing half of `Vegetation.js` bit-exactly (32 m spatial grid, 4 m `clearMask`, `clearRect`/`clearCircle`/`clearOriented`/`clearPolyline`/`isCleared`/`resnap`/`aliveCount`) and the renderer fires the site's own events: every demo road clears its corridor (+3 m), every demo block clears its footprint (+1.5 m; service pads 16×16 +5 m), the ROAD/SERVICE tools clear at placement, and the tree instance buffer repacks (Vegetation.update() skips !alive). The FULL undergrowth is ported: `LeafTextures.undergrowthAtlas` (the 8-cell Canvas atlas: fescue / bent-grass with seed heads / dry straw / fern / meadow sheet / weeds-clover / wildflower / dry sheet, same seeded blade geometry + 6-pass alpha dilation), the camera-focused 64 m turf patch placed bit-exactly (per-cell rng chain `hash2(hash2(seed, cx·73856093), cz·19349663)`, the meadow/fern density model from groundInfo, the near-focus multiplier, the ground-tint blend with the real per-layer mean colours from the CC0 JPEGs), drawn as instanced crossed cards with up-facing normals, the root-shadow gradient, MIP alpha boost 0.45, the stochastic alpha test at 0.42, the camera-radius fade and the SKY_FLOOR clamp. Pinned by `VegClearParityTest` (clears + two turf patches, one over a cleared footprint, one fern-rich) against `tools/probe_vegclear.mjs` driving the REAL module.
15. **Native street lamps + WetLights + GroundFXPass — DONE.** The site's ROAD_TYPES lamp specs place street lamps (32 m spacing, alternating sides, poleLat = cwHalf+0.85, 2 m arm, 9 m) and highway masts (46 m, median, 14 m) along the demo roads with emissive bulb heads. `worldgen/WetLights.kt` is a bit-exact port of `effects/WetLights.js` (mirrored-plane locus prominence ranking, cosA < 0.2 cull, 4 red-tail slot reservation, exposure-recovered vehicle glare radiance; vehicle glares authored from the traffic sim's head/tail lamps with the web's headI/tailI formulas). `TerrainShaders.FS_GROUNDFX` is the FULL `GroundFXPass.js` fragment shader (GLSL ES 3.0): screen-space wet reflections marched against the depth buffer (PuddleField drainage-map pool sharpening + the fxPuddleMask world-noise pools off-road, night clamp with the pool exemption), analytic 12-emitter streak smears (p9 sign fix, p10 att, p11 hot core, p13 near-field dilution), contact shadows (12-step sun march), 8-tap hemisphere AO with the 2×2 Bayer rotation, the contact-dark-first occlusion split, aerial perspective (day-lift/night-collapse) and the night light-pollution horizon band — running on a scene FBO (RGBA8 colour + D24) that the whole scene renders into. Pinned by `WetLightsParityTest` against `tools/probe_wetlights.mjs` on the real module.
16. **Native analytic lamp pools — DONE (diffuse half).** The road ribbons re-baked per polyline with their road-space frame (lat/along/dA/dB in a fourth vertex attribute); `FS_LIT_WET` evaluates the site's `rd_addLamp` inverse-square pool for the 3 nearest mid-block lamps of the fragment's road: the ±18 % per-luminaire variance keyed off the instance index, the 3 m trimmed-end margins (LAMP_MARGIN), the radius window (11/13/21 m per type) and the 180-620 m "string of beads" falloff, gated by uNight — the view-dependent GGX streak half of the web term arrives through the GroundFX emitter pass.
17. **Native temporal cloud history — DONE.** `FS_CLOUDS` gained the web's exponential temporal accumulation: the ray-march renders into an exact-1:2 half-res RGBA16F ping-pong target (RGBA8 fallback where float renderability is missing), reprojections by direction through the rotation-only view-projection, jitters with the stratified golden-ratio sequence (the fixed-jitter probe path remains the no-history fallback), and composites through the Gaussian 4×4 reconstruction (σ 0.75 texel) with depth-test occlusion and premultiplied blending. Weather changes drop the history (Clouds.resetHistory).

18. **Native street furniture — DONE.** `worldgen/Props.kt` is a bit-exact port of the street-furniture half of `props/PropScatter.js` (bus stops, street trees in pits, benches + bins, litter bins, hydrants, avenue planters / cycle stands / news boxes, mid-block regulatory signs, utility cabinets, park-path lamps + benches + bins + shrubs, kerbside parked cars with the 16-colour palette) driven by the same rng chain `makeRng(hash2(seed, hashString('props:' + seg.id)))` over the same per-(segment, side, channel) arc-length occupancy — pinned by `PropsParityTest` against `tools/probe_props.mjs` driving the REAL module (274 items, every kind count + position exact; the one legitimate cross-runtime tolerance is `atan2` ulp on yaw, kept out of the checksum). The renderer draws box compositions in the site's palette, mulch pit decals, and palette-tinted parked cars through the vehicle path; street trees join the instanced forest (PropScatter kinds → the 9 palettes). Wiring caveat kept faithful: the road's own lamps hold their kerb slots (roadLamps claims (i+½)·spacing ±1.1 m) before anything else is placed.
19. **Native facade rhythm — DONE (parameters).** The building shader now evaluates the site's facade layout (buildings/generators.js `fp()` + facadeShader styles): per-building floor height / ground-floor height / bay width / window fraction / night lit-bias derived from the generator ranges and seeded by the building's own seed (same building → same facade), retail shopfront glazing on the ground floor, curtain-wall towers with spandrel bands, floor-slab shadow lines. (The full procedural mass generators — wings, penthouses, rooftop plant — remain a follow-up slice; the native keeps its box masses.)
20. **CI: conditional browser loop.** The Browser verification loop runs only when web sources change (src/ | public/ | index.html | vite.config.js | package.json | tools/check|shot | .github/workflows) — the bundled dist is byte-identical otherwise. The native build + dual emulator gate still run on every push.

21. **Native composer tail — DONE (HDR + bloom + grade + AgX + SMAA).** The Engine.js post stack is now the native frame graph's tail: the scene renders into an RGBA16F HDR target with a samplable DEPTH_COMPONENT24 texture (the site's EffectComposer target is HalfFloat + DepthTexture; the old RGBA8 + depth *renderbuffer* could not be sampled, so the GroundFX depth march silently read the colour texture, and the old 3-vertex fullscreen triangle left the upper-left half of every fullscreen pass unrasterised — both fixed), Water.js' reflection RT became HalfFloat, and the K_LIGHT/K_SKY=0.25/0.32 display-key folding is RETIRED (K=1: every uniform carries the web's literal radiance×exposure, tone-mapped by the real AgX). The tail: GroundFX blit → fxFbo (RGBA16F) → precipitation composited after the blit (EffectsPass.fxScene ordering) → the EffectsPass 13-tap sun-occlusion probe (1×1) → the full UnrealBloomPass (threshold 0.92 / strength 0.28 / radius 0.55, kernel radii 6/10/14/18/22 with the exact gaussian coefficients, 5-mip separable chain, factor-lerped composite, additive CopyShader blend onto the frame) → the ColorGradingPass 256-tap luma meter (1×1 RG16F ping-pong, exp(−1.5·dt) adaptation) → the FULL ColorGradingPass fragment (white-point anchoring, log S-curve contrast/toe/shoulder, soft black, saturation + mid-boost + highlight desat, split toning, lift/gain, vignette, the sun glare core/halo/streak/3 chromatic ghosts occluded by the probe) → three's AgX tone mapping + sRGB OETF (OutputPass) → SMAAPass with the EXACT area/search LUTs from three (extracted to APK assets, un-premultiplied on upload) blending at the letterbox viewport. `worldgen/GradeFx.kt` is a bit-exact port of the effects/index.js driver (rainAmt/snowAmt/wetness/snowCover damp chains + every grade uniform transition + the auto-exposure white point wPre + sun screen position/visibility), pinned by `GradeParityTest` against `tools/probe_grade.mjs` running the REAL Weather.js + atmosphere modules (5 presets × 4 hours, 20 rows). The heavy CPU bakes (Milky Way cube + moon map, 64³ cloud noise) moved to worker threads with GL-thread uploads, and the renderer logs init milestones + a frame-1 marker + a 15 s centre-patch pixel probe; the CI gate now POLLS for the frame-1 marker (up to 300 s) and FAILS on a uniform (blank) screenshot — the old fixed-sleep gate kept passing the unpresented-surface "white screen" era.

22. **Native VehicleSpray — DONE.** `worldgen/Sprites.kt` is a bit-exact port of the effect sprites (makeSpray / makeRing / makeSnowflake / makeRainStreak) plus the VehicleSpray xorshift seed layout, pinned by `SprayParityTest` against `tools/probe_sprites.mjs` running the REAL modules. The renderer draws the wake as instanced billboards (corner quad + per-instance emit/vel/seed, `glVertexAttribDivisor`, `glDrawElementsInstanced`) with the full VS/FS port: puffs left behind at the vehicle's own speed, fanning, rise-and-settle, growth, the anchor-above-the-road fix, the depth-soft occlusion against the REAL scene depth texture, exp2 fog attenuation, the sky×1.30 + warm night tint and the 2.8×(0.45+0.55·rain) opacity — synced per frame from the TrafficSim vehicle list (44 emitters × 36 puffs, 150 m range, the exact speed/strength gates), drawn into fxFbo after precipitation (the fxScene ordering).

23. **Native SmokeSystem — DONE.** `worldgen/Smoke.kt` is a bit-exact port of `effects/SmokeSystem.js` (PUFF_KINDS, countFor, build: budget thinning with opacity compensation, dust rect-perimeter spawn, burst clustering, tint/opacity jitter; the rebuild rng stream `makeRng(seed ^ 0x3ffec7)`) plus `sprites.js makeSmokeAtlas` (the 4×4 cauliflower lobe atlas: blob cluster + 3-octave threshold erosion + fine wisp octave + lobe pseudo-normal RGB + pow-smoothstep coverage alpha — baked on the async worker like the stars/clouds). The renderer runs the full VS/FS port: GPU ring-buffer loop puffs (age = fract(uTime/life + phase) — a plume is in steady state from frame 0), drag-limited injection, buoyancy settle, wind entrainment (τ 3.5 s), coherent emission-time turbulence (the column meanders as one body), delayed growth with optical-mass conservation, the lobe-lighting fragment (terminator wrap by optical density, forward scatter, backlit rim, dense-core self-shadow, sky multiple-scattering + ground bounce, up to 4 local lights fed from the nearest street-lamp bulbs), scene-depth soft occlusion and exp2 fog — drawn into fxFbo after precipitation. The emitter scan follows `effects/index.js rebuild()` branch order: recorded stacks/vents for industrial blocks, cold chimneys on res-low (`temp < 10 || snowing || snowCover > 0.3`, the web's cold flag), and NO guessed plumes where the site publishes none; `BlockBuilding` gained optional stacks/vents records for the buildings-module contract. Pinned by `SmokeParityTest` against `tools/probe_smoke.mjs` running the REAL web SmokeSystem (2112 instances, per-attribute float sums + verbatim heads + atlas byte checksums).

24. **Native HUD = the site's UI — DONE (structure slice).** The five-button Canvas HUD was replaced by a port of the site's interface (src/modules/ui): topbar (crest + editable city name with the rename dialog, milestone badge + progress bar with next-milestone text, date from the site's 1 May 2026 epoch with weekday, the 24 h day dial with the sun/moon marker (12:00 top, clockwise), clock digits + dayPhase, the Pause·1×·2×·4× speed group wired to a real simSpeed (0/1/2/4) that scales the whole tick, treasury + net/week, population + jobs, happiness % + colour-coded meter, notifications bell with unread badge, settings gear) — the bottom dock with the site's five CATEGORIES and the item tray carrying the catalog (4 ROAD_TYPES with ¤/m costs + economy.js ROAD_COST upkeep, 6 ZONE types with the CS2 colours, the 8 SERVICE_TYPES with real costs/radii, bulldoze, 7 INFO_VIEWS) — the RCI+O demand bars fed by the economy's demand incl. office — the active-tool pill (name, category, cost, tap = Esc-cancel) — the toast stack + notifications centre (milestones/alerts feed drained thread-safely from the sim tick; active issues listed live) — the settings sheet (weather chips, time-of-day drag slider, hide-HUD cinematic toggle, city rename, web-parity shortcut) — and the building info panel. Tool plumbing: `tapTool(x, y, tool, opt)` takes catalog ids; roads persist per-cell types with per-type widths/colours/meshes and per-metre build cost + 50 % refund; zones 0..5 map to the growth types incl. res-high/com-high/office; services place a pinned type. Remaining UI-adjacent gaps: INFO_VIEWS terrain overlays render a stub message (next slice), perfguard auto-quality, the start menu.

## Native coverage audit (2026-09-08)

Question: "Посмотри абсолютно всё из сайта перенесено ли в нативное приложение?" — honest answer, module by module:

**Bit-exact ports (pinned by 18 JVM golden suites, 57 tests):**
- `shared/random.js`, `shared/noise.js`, `shared/math.js` (V8 quirks incl. Math.hypot fdlibm) → `Rng.kt`/`SimplexNoise.kt`/`V8Math.kt`
- `terrain/Heightmap.js` (analytic generator, conformPath/flattenRect/conformDisc, shore EDT SDF, CoarseGrid horizon rings, getHeightAny) → `Heightmap.kt`
- `terrain/GroundControl.js` (512² control map) → `GroundControl.kt`
- `terrain/TerrainMaterial.js` — the FULL 8-layer splat fragment shader (FS_TERRAIN) + the 8 CC0 PBR layer arrays + fillNormals; terrain:changed re-bake pipeline
- `terrain/Water.js` (chunk scan, 18-wave normal map, planar reflection RT, rain dimples, waterline SDF refresh)
- `terrain/Vegetation.js` (distribute + full clearing model + undergrowth turf patch) + `terrain/textures.js` (leaf cards, 8-cell undergrowth atlas, noise/water-normal textures)
- `demo/DemoCity.js` (u,v road layout, trumpet interchange, blocks, routes)
- `environment/atmosphere.js` (single-scattering sky, ephemeris, exposure), `environment/Weather.js` (5 presets, damp chains, wind wander), `environment/StarField.js` (Milky Way cube + moon map), `environment/Clouds.js` (+ temporal history), `environment/CloudShadow.js`
- `effects/PuddleField.js`, `effects/GroundFXPass.js` (SSR + emitter streaks + AO + contact shadows), `effects/WetLights.js`, `effects/WetSurfaces.js` (wet/snow grades), `effects/ColorGradingPass.js` + `effects/EffectsPass.js` + `effects/index.js` driver (`GradeFx.kt`), `effects/Precipitation.js`, `effects/VehicleSpray.js` + sprites, **`effects/SmokeSystem.js` (this slice)**
- `traffic/LaneNetwork.js` (Bézier connectors, junctions/signals/conflict matrix, A* router, pedestrian network), `traffic/TrafficSim.js` (IDM, claims, congestion), `traffic/VehicleModels.js`
- `simulation/services.js`, `simulation/economy.js`, `simulation/milestones.js`, `simulation/clock.js` (the sim core)
- `props/PropScatter.js` (street-furniture scatter rules)
- The `Engine.js` post stack (UnrealBloom, ColorGrading, AgX Output, SMAA with three's exact LUTs)

**Structurally ported (faithful look, documented simplification):**
- Building masses: box bodies + the site's facade rhythm parameters (generators.js floor/bay/shopfront/curtain-wall rules). The full procedural mass generators (wings, penthouses, rooftop plant, recorded stacks/vents for non-industrial) remain the largest visual follow-up.
- Road rendering: ribbons with the site's ROAD_TYPES cross-sections + lamp pools; the full RoadMesher intersection/marking geometry is simplified.
- Street furniture/props: box compositions in the site's palette (the site uses GLTF models).
- Terrain mesh: single 320² grid (6.4 m quads) vs the site's chunked LOD.
- UI/HUD: intentionally redesigned for touch (the site's desktop toolbar lives in the Parity track).

**Not ported natively (gaps, in priority order):**
1. `modules/audio/*` — the entire adaptive soundscape (14 modules: mixer, traffic/zone emitters, UI sounds, DSP, offline buffers). The native track is silent.
2. Zone-driven building growth (`zoning/ZoneGrid.js` → construction): zones feed the economy but no buildings grow on them yet; consequently the SmokeSystem dust path (construction) has no source either.
3. `demo/Landmarks.js` + `demo/DistrictGround.js` + `demo/Infrastructure.js` (rail, port, motorway dressing) — the demo city's landmark silhouettes and district ground painting.
4. `modules/menu/*` — start menu (backdrop city, minimap, city-name generator). The Parity track has the full menu.
5. `modules/perfguard/*` — auto quality scaling (the native ships frame-time instrumentation but fixed budgets).
6. `environment/EnvProbe.js` / `MaterialHook.js` — probe-driven material feedback (wetness grades already covered by WetSurfaces ports).
7. `modules/simulation/recipes.js` — recipe unlock chain (verify coverage when growth lands).

The Parity track (`ParityActivity`) serves the complete production web build offline, so it carries ALL of the above 1:1; the gaps listed are native-track only.
