# AndroidX flags (no legacy support-library dependencies; the web-parity WebView is gone).
android.useAndroidX=true
# No legacy support-library dependencies in this project.
android.enableJetifier=false
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
